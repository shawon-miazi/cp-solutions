#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while (t--)
    {
        int n,x,a;
        cin>>n>>x>>a;
        vector<vector<int>>arr(x,vector<int>(2,0));
        string s;
        cin>>s;
        for (int i=0;i<n;i++)
        {
            char c=s[i];
            for (int j=0;j<x;j++)
            {
                
            }
        }
    }
}