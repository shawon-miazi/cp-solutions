#include<stdio.h>
#include<math.h>
int main()
{
    int t;
    scanf("%d",&t);
    for (int w=0;w<t;w++)
    {
        int x,y;
        scanf("%d%d",&x,&y);
        int a=0,b=0,max=0;
        if (x>y)
        printf("0 0\n");
        else 
        {
            for (int i=1;i<y;i++)
            {
                for (int j=i;y>max;b++)
                {
                    
                    if () 
                }
                if (y%(x*i)==0)
                {
                    
                }
            }
        }
    }
    return 0;
}
