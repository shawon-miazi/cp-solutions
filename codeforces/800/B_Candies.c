#include<stdio.h>
#include<math.h>
int main()
{
    int t;
    scanf("%d",&t);
    for (int w=0;w<t;w++)
    {
        int n,sum=0,arr[40]={0};
        scanf("%d",&n);
        if (n%2==0)
        printf("-1\n");
        else 
        {
            sum+=3;
            arr[0]=2;
            for (int i=1;i<n;i++)
            {
                
            }
        }
    }
    return 0;
}